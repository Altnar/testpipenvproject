from setuptools import setup

setup(
   name='testpiepnv',
   version='1.0',
   description='just try to make pipenv work',
   author='ME!',
   author_email='ME!@foo.com',
   packages=['some_things'],  #same as name
)